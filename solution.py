import numpy as np

chat_id = 632165934  # Ваш chat ID, не меняйте название переменной
CONST = 0.03


def solution(x_success: np.array,
             x_cnt: np.array, ) -> bool:  # Одна или две выборке на входе, заполняется исходя из условия
    return True
